# SolRatio — Dipendenze Python
# Installazione:  pip install -r requirements.txt
#
# DIPENDENZA ESTERNA NON-PIP (richiesta!):
#   Radiance >= 5.4    https://www.radiance-online.org/
#   I comandi gendaylit, oconv, rtrace devono essere nel PATH di sistema.

# === Core scientifico ===
numpy>=1.20
pandas>=1.3
pvlib>=0.10

# === Simulazione 3D ray-tracing ===
# Pin ESATTO (non >=): il motore dipende dalla normalizzazione di
# makeScene1axis di questa versione (forma canonica della scena tracking) e
# porta un workaround specifico per analysis1axisground 0.5.1 (append=False,
# altrimenti KeyError silenzioso nel riferimento canonico della validazione).
# Verificare la forma scena e il workaround PRIMA di salire a 0.6.x.
bifacial_radiance==0.5.1

# === I/O Excel ===
openpyxl>=3.0
lxml>=4.6        # editing XML interno .xlsx (write_effetto_bordo)

# === Reportistica PDF (opzionale ma consigliata) ===
reportlab>=3.6
matplotlib>=3.4  # grafici nel PDF
